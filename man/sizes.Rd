% Generated by roxygen2 (4.0.1): do not edit by hand
\name{sizes}
\alias{sizes}
\title{Get all the available sizes that can be used to create a droplet.}
\usage{
sizes(what = "parsed", page = 1, per_page = 25, config = NULL)
}
\arguments{
\item{what}{(character) One of list or raw}

\item{config}{Options passed on to httr::GET. Must be named, see examples.}

\item{page}{Page number (Default: 1)}

\item{per_page}{Number of items per page (Default: 25)}
}
\value{
A data.frame with available sizes (RAM, disk, no. CPU's) and their costs
}
\description{
Get all the available sizes that can be used to create a droplet.
}
\examples{
\dontrun{
sizes()
sizes(per_page=2)
res <- sizes('raw')
res$url
res$headers
}
}

